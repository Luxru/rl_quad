<launch>
  <!-- size of map, change the size in x, y, z according to your application -->
  <arg name="map_size_x" value="40.0"/>
  <arg name="map_size_y" value="20.0"/>
  <arg name="map_size_z" value=" 5.0"/>

  <!-- topic of your odometry such as VIO or LIO -->
  <arg name="odom_topic" value="/state_ukf/odom" />

<!-- use simulator -->
  <include file="$(find rl_control_test)/launch/simulator.xml">
    <arg name="map_size_x_" value="$(arg map_size_x)"/>
    <arg name="map_size_y_" value="$(arg map_size_y)"/>
    <arg name="map_size_z_" value="$(arg map_size_z)"/>
    <arg name="c_num" value="10"/>
    <arg name="p_num" value="150"/>

    <arg name="odometry_topic" value="$(arg odom_topic)" />
  </include>

<node pkg="rl_control_test" name="rl_test" type='rl_test'>
<remap from="/position_cmd" to="planning/pos_cmd"/>
<remap from="/odom_world" to="$(arg odom_topic)"/>
<remap from="/cloud" to="/pcl_render_node/cloud"/>
</node>

</launch>
